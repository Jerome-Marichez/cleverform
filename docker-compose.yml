# Exécution locale "compatibilité" : app (image Docker) + PostgreSQL.
# Permet de vérifier que le frontend/backend tournent hors Vercel.
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cleverform
      POSTGRES_PASSWORD: cleverform
      POSTGRES_DB: cleverform
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  app:
    build: .
    environment:
      DATABASE_URL: postgresql://cleverform:cleverform@db:5432/cleverform?sslmode=disable
      DIRECT_URL: postgresql://cleverform:cleverform@db:5432/cleverform?sslmode=disable
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    ports:
      - "3000:3000"
    depends_on:
      - db

volumes:
  db-data:
